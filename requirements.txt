# Runtime. Upper bounds pin the major version so an unattended `docker build`
# months from now produces the same app that was tested.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-dotenv>=1.0,<2.0
python-multipart>=0.0.9,<1.0
pypdf>=4.0,<7.0
python-docx>=1.1,<2.0
openpyxl>=3.1,<4.0
